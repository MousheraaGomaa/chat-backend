

const USER_STATUSES = Object.freeze({
    ACTIVE: 'Active',
    BUSY: 'Busy',
    AWAY: 'Away',
    OFFLINE: 'Offline'
});

const MESSAGE_TYPES = Object.freeze({
    VIDEO: 'video',
    AUDIO: 'audio',
    TEXT: 'text',
    IMAGE: 'image'
})



export {
    USER_STATUSES,
    MESSAGE_TYPES
}